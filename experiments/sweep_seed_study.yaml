# experiments/sweep_seed_study.yaml

imports:
  - configs/base.yaml
  - configs/scenario/standard.yaml

# 방법론
method: vib

# sweep 모드
sweep_mode: full    # full | single

exp_id: seed_repro
sweep:
  seed: [0, 1, 2]
