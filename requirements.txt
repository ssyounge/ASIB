# --------------------------------------------------------------------
#  ★ 하위 실행에 필요한 top-level 패키지만 슬림화   (Python 3.12)
# --------------------------------------------------------------------
einops==0.8.1
hydra-core==1.3.2
omegaconf==2.3.0
numpy==1.26.4           # NumPy 2.x 런타임 충돌 회피
pandas==2.3.1
pillow==11.3.0
scikit-learn==1.7.1
sentry-sdk==2.32.0                   # (선택) W&B 내부 의존
timm==1.0.17
tqdm==4.67.1
wandb==0.21.0
# EfficientNet-PyTorch 더 이상 사용하지 않음

# ------------  GPU wheel은 cuda 버전에 맞추어 **별도** 설치 ------------
#  예) CUDA 12.1         : pip install torch==2.2.0+cu121 torchvision==0.17.0+cu121 -f https://download.pytorch.org/whl/cu121
#  예) CUDA 11.8         : pip install torch==2.2.0+cu118 ...
# --------------------------------------------------------------------

#  프로젝트 소스 (editable install)
-e git+https://github.com/ssyounge/ASMB_KD.git@6e5d84697b22e2be2650e8e71c2f0372797b217e#egg=asmb_kd
