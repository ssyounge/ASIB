# configs/experiment/kd_overlap/res152_dkd_overlap.yaml
defaults:
  - kd_overlap/res152_synergy_overlap   # 상속
  - _self_

kd_mode: dkd          # 코드 내부 if‑else 분기
dkd_temp: 4.0
dkd_alpha: 1.0        # target 분해 CE
dkd_beta: 8.0         # non‑target 분해 CE
kd_ens_alpha: 0.0     # synergy 미사용
use_distillation_adapter: false
